<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>Luume</title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

    <!-- Le styles -->
    <style>
      body {
        padding-top: 60px; /* 60px to make the container go all the way to the bottom of the topbar */
      }
    </style>
    <%= stylesheet_link_tag "application", :media => "all" %>

    <!-- Le fav and touch icons -->
    <link href="/images/favicon.ico" rel="shortcut icon">
    <link href="/images/apple-touch-icon.png" rel="apple-touch-icon">
    <link href="/images/apple-touch-icon-72x72.png" rel="apple-touch-icon" sizes="72x72">
    <link href="/images/apple-touch-icon-114x114.png" rel="apple-touch-icon" sizes="114x114">
  </head>
  <body>

    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
          <div class="container">
          <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <a class="brand" href="/">Luume</a>
          <div class="container nav-collapse">
            <ul class="nav">
              <% if logged_in? %>
                <li <% if ["users"].include?(controller.controller_name) %>class="active"<%end%>>
                <%= link_to profile_path do %>
                  <i class='<% if ["users"].include?(controller.controller_name) %>icon-white<%end%> icon-home'></i> Profile 
                <% end %>
                </li>
                <li <% if ["clients"].include?(controller.controller_name) %>class="active"<%end%>>
                <%= link_to clients_path do %>
                  <i class='<% if ["clients"].include?(controller.controller_name) %>icon-white<%end%> icon-user'></i> Clients
                <% end %>
                </li>
                <li <% if ["projects","tasks","invoices"].include?(controller.controller_name) %>class="active"<%end%>>
                <%= link_to projects_path do %>
                  <i class='<% if ["projects","tasks","invoices"].include?(controller.controller_name) %>icon-white<%end%> icon-th-large'></i> Projects 
                <% end %>
                </li>
                <li> 
                <%= link_to logout_path do %>
                  <i class='icon-off'></i> Log out 
                <% end %>
                </li>
              <% else %>
                <li> 
                <%= link_to login_path do %>
                  <i class='icon-ok'></i>Log in 
                <% end %>
                </li>
                <li> 
                <%= link_to signup_path do %>
                  <i class='icon-arrow-up'></i>Sign Up 
                <% end %>
                </li>
              <% end %>
            </ul>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>

    <div class="container">
      <div class="content">
        <div class="row">
          <% flash.each do |key, msg| %>
            <div class="alert alert-<%= key %>">
              <a class="close" data-dismiss="alert">×</a>
              <%= msg %>
            </div>
          <% end %>
        </div>
        <div class="row">
            <%= yield %>
        </div><!--/row-->
      </div><!--/content-->

    </div> <!-- /container -->

    <!-- Le javascript
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <script type ="text/javascript" src="/js/jquery.js"></script>
    <script type ="text/javascript" src="/js/jquery.tablesorter.js"></script>
    <%= javascript_include_tag "application" %>
    <%= javascript_include_tag "site" %>
  </body>
</html>
